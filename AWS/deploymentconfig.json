{"django": {
  "image": ":postflow.django.latest",
  "ports": {"8000": "HTTP"},
  "volumes": [
      "/staticfiles:/staticfiles"  # Shared volume for static files
    ]
  },
  "nginx": {
    "image": ":postflow.nginx.latest",
    "ports": {"80": "HTTP"},
    "volumes": [
      "/staticfiles:/staticfiles"  # Shared volume for static files
    ]
  }
}
