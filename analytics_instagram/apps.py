from django.apps import AppConfig


class AnalyticsInstagramConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'analytics_instagram'
    verbose_name = 'Instagram Analytics'
